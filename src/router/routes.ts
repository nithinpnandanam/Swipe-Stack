const ROOT_PATH = '/'
const LOGIN = '/login'
const SIGN_UP = '/sign-up'
const EDIT_PROFILE = '/edit-profile'
const paths = {
    ROOT_PATH,
    LOGIN,
    SIGN_UP,
    EDIT_PROFILE
} as const

export default paths